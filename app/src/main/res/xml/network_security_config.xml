<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- We scrape a lot of data, so we need to access http sites -->
    <base-config cleartextTrafficPermitted="true"/>
</network-security-config>